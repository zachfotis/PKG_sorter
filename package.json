{
  "name": "@foza/sorter",
  "version": "1.0.1",
  "description": "This is a short library for shorting various data structures",
  "main": "./build/index.js",
  "types": "./build/index.d.ts",
  "files": [
    "build/**/*"
  ],
  "scripts": {
    "clean": "del-cli build",
    "build": "npm run clean && tsc",
    "publish": "git add . && git commit -m \"Updates\" && npm version patch && npm run build && npm publish",
    "dev": "nodemon src/index.ts"
  },
  "keywords": [],
  "author": "Fotios N. Zachopoulos",
  "license": "ISC",
  "devDependencies": {
    "del-cli": "^5.0.0",
    "nodemon": "^2.0.21",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.5"
  }
}
